<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, prompt: "Select a movie", input_html: {data: {controller: "tom_select"}} %>

  <%# BEFORE TOM SELECT %>
  <%# <%= f.association :movie, as: :radio_buttons %>

  <%= f.submit "Submit bookmark", class: "btn btn-primary" %>
<% end %>
